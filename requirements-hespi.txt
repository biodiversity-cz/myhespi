# Závislosti pro myHESPI.
# Instalace: pip install -r requirements-hespi.txt
#
# Systémové závislosti (macOS):
#   brew install tesseract        # OCR engine
#   brew install tesseract-lang   # další jazyky (čeština, němčina, latina, …)
#   brew install xz               # nutné pro JP2 a komprimované modely (_lzma)
# Při použití pyenv nejdřív nainstalujte xz, pak sestavte Python s:
#   CPPFLAGS="-I$(brew --prefix xz)/include" LDFLAGS="-L$(brew --prefix xz)/lib" pyenv install 3.11.x
#
# HESPI (upstream) — stáhne si všechny ML závislosti automaticky
hespi>=0.6.0
# Flask web framework
Flask>=2.2,<3.0
Werkzeug>=2.2,<2.4
# Data
pandas>=2.0.0
# WSGI server
gunicorn>=21.0
# Vývoj
pytest>=7.0
